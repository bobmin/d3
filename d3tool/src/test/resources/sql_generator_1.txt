SELECT * FROM DOCUMENT 
INNER JOIN PROPERTY ON DOC_ID = PROP_DOC_ID AND PROP_LONGTEXT = 'Kunden-Nr.' AND PROP_VALUE IN ('11016','5000')
WHERE DOC_EXT IN ('PDF') 
ORDER BY DOC_NUMMER DESC
LIMIT 0, 30;
------------------------
knr = 5000 knr = 11016 ext = PDF
knr = 5000 ext = PDF knr = 11016